# MSP-TSK-260421001-001 — T1_retrieval-provider-interface

**Status**: PENDING
**Wave**: 0 (Foundation)
**Parent IMP**: [MSP-IMP-260421001](../implement/MSP-IMP-260421001.md)
**Implementer**: MSP-AGT-EVA-COWORK
**Estimate**: 1.5 hrs
**Depends on**: (none — first task)

---

## Concern

สร้าง `RetrievalProvider` interface + shared types (`Query`, `Hit`, `SearchOpts`, `ProviderHealth`) ตาม contract ใน `ADR--HYBRID-RETRIEVAL`

**One concern**: interface definition เท่านั้น ไม่รวม implementation

---

## Outputs

### Files created
| Path | Exports |
|---|---|
| `eva-cli/src/memory/providers/types.ts` | `RetrievalProvider` (interface), `Query`, `Hit`, `SearchOpts`, `ProviderHealth`, `ProviderKind`, `QueryMode` |

### Existing types to keep or migrate
| Current (from `src/types/memory.ts`) | Action |
|---|---|
| `Hit` | Extend to include `evidence` field, keep compat |
| `RetrievalQuery` | Mark deprecated, alias to new `Query` |
| `RetrievalResult` | Keep, used by facade |

---

## Acceptance Criteria

- [ ] `npx tsc --noEmit` passes with new types file
- [ ] `Hit` interface adds optional `evidence?: { exactMatch?, keywordCount?, cosineScore?, graphDistance? }`
- [ ] `Query.filters` supports `phase`, `type`, `status`, `scope`, `tags[]`
- [ ] `Query.relations` supports `seedIds`, `expandBacklinks`, `expandForwardlinks`, `depth`
- [ ] `Query.budget` supports `maxHits`, `maxLatencyMs`
- [ ] `RetrievalProvider.capability(q: Query)` returns `'miss' | 'may_hit' | 'definite_hit'`
- [ ] No runtime code — type definitions only
- [ ] Existing `src/types/memory.ts` **not modified** (backward compat)
- [ ] New file placed in `src/memory/providers/` (dir created)

---

## Test Plan

- [ ] **unit**: Import types in a dummy test file, assert shape (TypeScript compile = proof)
- [ ] **unit**: Create mock provider implementing interface — compile success = contract valid
- [ ] No runtime tests (pure types)

---

## Sample implementation reference

Boss ดูตัวอย่างได้จาก `ADR--HYBRID-RETRIEVAL#Provider contracts` — TSK นี้แค่ translate contract นั้นเป็นโค้ด TypeScript

---

## Risks

- **Risk**: Interface shape conflict กับ existing `memory.ts` types
- **Mitigation**: Put ใน sub-namespace `src/memory/providers/` ก่อน; merge names ใน Wave 5

---

## Notes

- ไม่ lock ไว้กับ Zod — interface Ts-level พอ (validation ทำใน provider impl)
- `ProviderHealth` minimal: `{ ok: boolean, latencyMs?: number, message?: string }`
- Avoid generic `<T>` — hit shape ควร concrete เพราะ provider ต่าง ๆ return hit เหมือนกัน

---

## Completion

Check when done:
- [ ] File created
- [ ] Types compile clean
- [ ] Update IMP checklist: `[x] T1_retrieval-provider-interface`
- [ ] Commit: `feat(memory): T1 add RetrievalProvider interface`
