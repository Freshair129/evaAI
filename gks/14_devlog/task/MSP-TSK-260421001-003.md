# MSP-TSK-260421001-003 — T3_atomic-index-provider

**Status**: IN_PROGRESS
**Wave**: 1 (Providers)
**Parent IMP**: [MSP-IMP-260421001](../implement/MSP-IMP-260421001.md)
**Implementer**: MSP-AGT-EVA-COWORK
**Estimate**: 2 hrs
**Depends on**: T1

---

## Concern

Implement `AtomicIndexProvider` สำหรับการค้นหาแบบ Exact ID matching (O(1)) และ Substring ID (O(N)) โดยใช้ข้อมูลจาก `gks/00_index/atomic_index.jsonl`

**One concern**: Memory efficiency — โหลด index เข้า Map ครั้งเดียวตอน startup หรือตอนตรวจพบว่าไฟล์เปลี่ยน (fs.watch)

---

## Outputs

### Files created
| Path | Exports |
|---|---|
| `eva-cli/src/memory/providers/atomic.ts` | `AtomicIndexProvider` |

---

## Design

```typescript
export class AtomicIndexProvider implements RetrievalProvider {
  readonly kind = 'atomic'
  readonly cost = 'O(1)'

  private index = new Map<string, IndexEntry>()

  constructor(private indexPath: string) {}

  async load() {
    // Read atomic_index.jsonl line by line
    // Parse JSON and populate this.index (Map<id, entry>)
  }

  capability(q: Query): Capability {
    if (q.mode === 'exact' || /^[A-Z]+--[A-Z0-9-]+$/.test(q.text)) {
      return 'definite_hit'
    }
    return 'may_hit'
  }

  async search(q: Query, opts?: SearchOpts): Promise<Hit[]> {
    // 1. Exact lookup
    const entry = this.index.get(q.text)
    if (entry) {
      return [{
        source: 'atomic',
        id: entry.id,
        path: entry.path,
        score: 1.0,
        snippet: `${entry.id} (${entry.phase}/${entry.status})`,
        evidence: { exactMatch: true }
      }]
    }

    // 2. Substring matching (fallback)
    // ...
  }

  async health(): Promise<ProviderHealth> {
    return { ok: true }
  }
}
```

---

## Acceptance Criteria

- [ ] `AtomicIndexProvider` implements `RetrievalProvider` correctly
- [ ] exact match คืน score 1.0 และ `evidence.exactMatch: true`
- [ ] substring match คืน score 0.7
- [ ] สนับสนุน filters (phase, type, status)
- [ ] Performance: exact lookup < 1ms
- [ ] Unit tests ครอบคลุม hit/miss cases

---

## Test Plan

- [ ] **unit**: `atomic.test.ts`
- [ ] Test exact match with valid ID
- [ ] Test substring match
- [ ] Test filters (e.g. only phase 2)
- [ ] Test health check

---

## Completion

Check when done:
- [ ] Implementation completed
- [ ] Unit tests passed
- [ ] Update IMP checklist: `[x] T3_atomic-index-provider`
- [ ] Commit: `feat(memory): T3 implement AtomicIndexProvider`
